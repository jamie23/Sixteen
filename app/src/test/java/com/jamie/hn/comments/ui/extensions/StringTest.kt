package com.jamie.hn.comments.ui.extensions

class StringTest {
    // Currently, we cannot test the extensions functions for string due to them
    // as all extension functionality relies on Android components
}
